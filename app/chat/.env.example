# Copy to .env for local development (.env is gitignored — NEVER commit it).
# In production ANTHROPIC_API_KEY comes from Secret Manager on Cloud Run.

# Required for /api/chat. Server-side only; never ships to the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional — defaults shown.
#PORT=8080
#ROUTER_GRAPHQL_URL=https://cosmo-router-dkuqnmldta-uc.a.run.app/graphql
